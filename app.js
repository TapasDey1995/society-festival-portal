import { createClient } from 'https://esm.sh/@supabase/supabase-js@2.57.4';
const SUPABASE_URL='https://suvwxkjytbmpxaqovulq.supabase.co';
const SUPABASE_KEY='sb_publishable_lY775k5ntfdC5TnfhfBJLg_ioQaD1iY';
const SOCIETY_ID='5917571c-e36e-44b1-898a-212b8989c6ff';
const LOGIN_EMAIL='tapas@meenaorchid.local'; const TOTAL_FLATS=136;
const USERNAME_EMAILS={tapas:'tapas@meenaorchid.local',committee:'committee@meenaorchid.local'};
const DOCUMENT_BUCKET='festival-documents';
const supabase=createClient(SUPABASE_URL,SUPABASE_KEY); const $=id=>document.getElementById(id);
const money=n=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:2}).format(Number(n||0));
let festivals=[],members=[],collections=[],expenses=[],schedules=[],profile=null; let editingCollectionId=null;
function expenseNumbers(x){const total=Number(x.total_amount||0),advance=Number(x.advance_amount||0),status=x.expense_status||'Pending';return status==='Clear'?{paid:total,remaining:0}:{paid:advance,remaining:Math.max(0,total-advance)};}
async function load(){try{const[f,m,c,e,s]=await Promise.all([supabase.from('festivals').select('*').eq('society_id',SOCIETY_ID).order('sort_order'),supabase.from('members').select('*').eq('society_id',SOCIETY_ID).order('block_no').order('flat_no'),supabase.from('collections').select('*').order('collection_date',{ascending:false}).order('created_at',{ascending:false}),supabase.from('expenses').select('*').order('expense_date',{ascending:false}),supabase.from('puja_schedules').select('*').order('schedule_date').order('sort_order')]);if(f.error)console.error('Festivals load error:',f.error);if(m.error)console.error('Members load error:',m.error);if(c.error)console.error('Collections load error:',c.error);if(e.error)console.error('Expenses load error:',e.error);if(s.error)console.error('Schedule load error:',s.error);festivals=f.error?[]:(f.data||[]);members=m.error?[]:(m.data||[]);collections=c.error?[]:(c.data||[]);expenses=e.error?[]:(e.data||[]);schedules=s.error?[]:(s.data||[]);const festivalMap=new Map(festivals.map(x=>[x.id,x])),memberMap=new Map(members.map(x=>[x.id,x]));collections=collections.map(x=>({...x,festivals:festivalMap.get(x.festival_id)||null,members:memberMap.get(x.member_id)||null}));expenses=expenses.map(x=>({...x,festivals:festivalMap.get(x.festival_id)||null}));schedules=schedules.map(x=>({...x,festivals:festivalMap.get(x.festival_id)||null}));renderAll();fillSelects();if(c.error)formMessage('collectionQuickMessage','Unable to load collection data: '+c.error.message,true);}catch(error){console.error('Portal data load error:',error);const msg=error?.message||String(error);if($('adminMessage'))$('adminMessage').textContent='Data loading error: '+msg;if($('summaryBody'))$('summaryBody').innerHTML=emptyRow(5,'Unable to load data. Please refresh.');if($('collectionBody'))$('collectionBody').innerHTML=emptyRow(isStaff()?14:13,'Unable to load collection data: '+msg);if($('expenseGroups'))$('expenseGroups').innerHTML='<p class="muted">Unable to load expense data.</p>';}}
function renderAll(){renderDashboard();renderCollections();renderExpenses();renderSchedule();}
function renderDashboard(){const totalCollected=collections.filter(x=>x.status!=='Cancelled').reduce((a,x)=>a+Number(x.amount||0),0),totalPaid=expenses.reduce((a,x)=>a+expenseNumbers(x).paid,0);if($('totalCollected'))$('totalCollected').textContent=money(totalCollected);if($('totalExpenses'))$('totalExpenses').textContent=money(totalPaid);if($('totalBalance'))$('totalBalance').textContent=money(totalCollected-totalPaid);const paidFlats=new Set(collections.filter(x=>x.status==='Paid'&&x.members?.block_no&&x.members?.flat_no).map(x=>`${x.members.block_no}-${x.members.flat_no}`)),count=paidFlats.size,pct=Math.min(100,Math.round(count/TOTAL_FLATS*100));if($('contributorCount'))$('contributorCount').textContent=`${count} / ${TOTAL_FLATS}`;if($('contributorPercent'))$('contributorPercent').textContent=`${pct}%`;const progress=$('contributorProgress');if(progress)progress.style.width=`${pct}%`;const summary=$('summaryBody');if(summary){const groups=festivals.map(f=>{const col=collections.filter(x=>x.festival_id===f.id&&x.status!=='Cancelled').reduce((a,x)=>a+Number(x.amount||0),0),festivalExpenses=expenses.filter(x=>x.festival_id===f.id),paid=festivalExpenses.reduce((a,x)=>a+expenseNumbers(x).paid,0),remaining=festivalExpenses.reduce((a,x)=>a+expenseNumbers(x).remaining,0);return{name:f.name,total_collection:col,total_expense:paid,remaining,balance:col-paid};});summary.innerHTML=groups.map(r=>`<tr><td><strong>${esc(r.name)}</strong></td><td>${money(r.total_collection)}</td><td>${money(r.total_expense)}</td><td>${money(r.remaining)}</td><td class="${r.balance>=0?'positive':'negative'}">${money(r.balance)}</td></tr>`).join('')||emptyRow(5,'No festival data');}}
function renderCollections(){const search=$('collectionSearch'),festivalFilter=$('collectionFestivalFilter'),typeFilter=$('collectionTypeFilter'),modeFilter=$('collectionModeFilter'),statusFilter=$('collectionStatusFilter'),body=$('collectionBody'),actionHead=$('collectionActionHead');if(!search||!festivalFilter||!typeFilter||!modeFilter||!statusFilter||!body||!actionHead)return;const q=(search.value||'').trim().toLowerCase(),festival=festivalFilter.value,type=typeFilter.value,mode=modeFilter.value,status=statusFilter.value;const rows=collections.filter(x=>{const text=`${x.festivals?.name||''} ${x.members?.block_no||''} ${x.members?.flat_no||''} ${x.members?.name||''} ${x.receipt_no||''}`.toLowerCase();return(!q||text.includes(q))&&(!festival||x.festival_id===festival)&&(!type||x.collection_type===type)&&(!mode||x.payment_mode===mode)&&(!status||x.status===status);});const staff=isStaff();actionHead.classList.toggle('hidden',!staff);body.innerHTML=rows.map((x,i)=>{const receipt=x.receipt_url||x.file_upload_url,download=x.receipt_download_url;return`<tr><td>${i+1}</td><td>${formatDate(x.collection_date)}</td><td>${esc(x.members?.block_no||x.block_no||'')}</td><td>${esc(x.members?.flat_no||x.flat_no||'')}</td><td>${esc(x.notes||x.members?.name||'')}</td><td>${money(x.amount)}</td><td>${esc(x.payment_mode||'')}</td><td><span class="status ${x.status==='Cancelled'?'cancelled':x.status==='Progress'?'progress-status':'paid'}">${esc(x.status||'')}</span></td><td>${esc(x.receipt_no||'')}</td><td>${esc(x.transaction_id||'')}</td><td>${receipt?`<a href="${safeUrl(receipt)}" target="_blank" rel="noopener">View file</a>`:'—'}</td><td>${esc(x.collection_type||'')}</td><td>${download?`<a href="${safeUrl(download)}" target="_blank" rel="noopener">Download</a>`:'—'}</td>${staff?`<td><button class="secondary edit-collection" data-id="${x.id}">Edit</button></td>`:''}</tr>`;}).join('')||emptyRow(staff?14:13,'No collections match the filters');body.querySelectorAll('.edit-collection').forEach(b=>b.onclick=()=>editCollection(b.dataset.id));}
function renderExpenses(){const search=$('expenseSearch'),filterEl=$('expenseFestivalFilter'),groupsEl=$('expenseGroups');if(!search||!filterEl||!groupsEl)return;const q=(search.value||'').toLowerCase(),filter=filterEl.value,selected=festivals.filter(f=>!filter||f.id===filter),staff=isStaff();groupsEl.innerHTML=selected.map(f=>{const rows=expenses.filter(x=>x.festival_id===f.id&&(!q||`${x.expense_name||''} ${f.name}`.toLowerCase().includes(q))),total=rows.reduce((a,x)=>a+Number(x.total_amount||0),0),paid=rows.reduce((a,x)=>a+expenseNumbers(x).paid,0);return`<div class="expense-group"><div class="group-head"><h3>${esc(f.name)}</h3><div class="expense-totals" style="display:flex;justify-content:space-between;align-items:center;gap:20px;"><span>Total Expenses: <strong>${money(total)}</strong></span><span>Expenses Done: <strong>${money(paid)}</strong></span></div></div><div class="table-wrap"><table><thead><tr><th>Sr No</th><th>Date</th><th>Expense Name</th><th>Total Amount</th><th>Advance</th><th>Expenses Done</th><th>Remaining</th><th>Status</th><th>Bill / Receipt File</th></tr></thead><tbody>${rows.map((x,i)=>{const n=expenseNumbers(x);return`<tr><td>${i+1}</td><td>${formatDate(x.expense_date)}</td><td>${esc(x.expense_name)}</td><td>${money(x.total_amount)}</td><td>${money(x.advance_amount)}</td><td>${money(n.paid)}</td><td>${money(n.remaining)}</td><td>${staff?`<select class="expense-status" data-id="${x.id}"><option ${x.expense_status==='Clear'?'selected':''}>Clear</option><option ${x.expense_status!=='Clear'?'selected':''}>Pending</option></select>`:`<span class="status ${x.expense_status==='Clear'?'paid':'progress-status'}">${esc(x.expense_status||'Pending')}</span>`}</td><td>${x.bill_url?`<a href="${safeUrl(x.bill_url)}" target="_blank" rel="noopener">View file</a>`:'—'}</td></tr>`;}).join('')||emptyRow(9,'No expenses in this category')}</tbody></table></div></div>`;}).join('')||'<p class="muted">No expense categories.</p>';document.querySelectorAll('.expense-status').forEach(s=>s.onchange=()=>updateExpenseStatus(s.dataset.id,s.value));}
async function updateExpenseStatus(id,status){const{error}=await supabase.from('expenses').update({expense_status:status,updated_at:new Date().toISOString()}).eq('id',id);finishAdmin(error,error?'':'Expense status updated.');if(!error)await load();}
function renderSchedule(){const body=$('scheduleBody');if(!body)return;body.innerHTML=schedules.map(x=>`<tr><td><strong>${esc(x.festivals?.name||'')}</strong></td><td>${formatDate(x.schedule_date)}</td><td>${esc(x.event_time||'')}</td><td>${esc(x.title)}</td><td>${esc(x.description||'')}</td></tr>`).join('')||emptyRow(5,'Schedule will appear here once entries are added');}
function fillSelects(){const opts=festivals.map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('');['collectionFestival','expenseFestival','scheduleFestival','quickCollectionFestival','quickExpenseFestival'].forEach(id=>{const el=$(id);if(el)el.innerHTML=opts;});if($('collectionFestivalFilter'))$('collectionFestivalFilter').innerHTML='<option value="">All festivals</option>'+opts;if($('expenseFestivalFilter'))$('expenseFestivalFilter').innerHTML='<option value="">All categories</option>'+opts;if($('collectionMember'))$('collectionMember').innerHTML='<option value="">General donation / no flat</option>'+members.map(x=>`<option value="${x.id}">${esc(x.block_no)} ${esc(x.flat_no)} — ${esc(x.name)}</option>`).join('');if($('quickCollectionMember'))$('quickCollectionMember').innerHTML='<option value="">General donation / no flat</option>'+members.map(x=>`<option value="${x.id}">${esc(x.block_no)} ${esc(x.flat_no)} — ${esc(x.name)}</option>`).join('');}
function emptyRow(n,text){return`<tr><td colspan="${n}" class="muted center">${text}</td></tr>`;}function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));}function safeUrl(v){return esc(String(v||'').trim());}function formatDate(v){if(!v)return'';const[y,m,d]=String(v).split('-');return d&&m&&y?`${d}/${m}/${y}`:v;}function slugFileName(name){return String(name||'file').replace(/[^a-zA-Z0-9._-]+/g,'-').replace(/-+/g,'-').slice(0,120);}
async function uploadDocument(file,folder){if(!file)return null;const allowed=['application/pdf','image/jpeg','image/png','image/webp'];if(!allowed.includes(file.type))throw new Error('Only PDF, JPG, PNG or WEBP files are allowed.');if(file.size>10*1024*1024)throw new Error('File must be 10 MB or smaller.');const safe=slugFileName(file.name),path=`${folder}/${Date.now()}-${crypto.randomUUID()}-${safe}`,{error}=await supabase.storage.from(DOCUMENT_BUCKET).upload(path,file,{cacheControl:'3600',upsert:false,contentType:file.type});if(error)throw error;return supabase.storage.from(DOCUMENT_BUCKET).getPublicUrl(path).data.publicUrl;}
function isStaff(){return!!profile&&['admin','treasurer','committee'].includes(profile.role);}function formMessage(id,text,error=false){const el=$(id);if(el){el.textContent=text;el.classList.toggle('negative',!!error);}}
async function refreshAuth(){const{data:{session}}=await supabase.auth.getSession();$('loginBtn').classList.toggle('hidden',!!session);$('logoutBtn').classList.toggle('hidden',!session);$('userBadge').classList.toggle('hidden',!session);profile=null;if(session){const{data}=await supabase.from('user_profiles').select('*').eq('id',session.user.id).maybeSingle();if(data)profile=data;else if(session.user.email===USERNAME_EMAILS.committee)profile={id:session.user.id,society_id:SOCIETY_ID,full_name:'Meena Orchid Committee',role:'committee'};else if(session.user.email===USERNAME_EMAILS.tapas)profile={id:session.user.id,society_id:SOCIETY_ID,full_name:'Tapas',role:'admin'};$('userBadge').textContent=profile?.full_name||session.user.email||'User';$('roleBadge').textContent=profile?.role||'resident';const staff=isStaff();$('adminTab').classList.toggle('hidden',!staff);if($('showCollectionForm'))$('showCollectionForm').classList.toggle('hidden',!staff);if($('showExpenseForm'))$('showExpenseForm').classList.toggle('hidden',!staff);renderCollections();renderExpenses();if(!staff&&location.hash==='#admin')showPage('dashboard');}else{$('adminTab').classList.add('hidden');$('collectionActionHead').classList.add('hidden');if($('showCollectionForm'))$('showCollectionForm').classList.add('hidden');if($('showExpenseForm'))$('showExpenseForm').classList.add('hidden');if(location.hash==='#admin')showPage('dashboard');}}
window.refreshAuth=refreshAuth;
async function addMember(ev){ev.preventDefault();const{error}=await supabase.from('members').insert({society_id:SOCIETY_ID,block_no:$('memberBlock').value.trim(),flat_no:$('memberFlat').value.trim(),name:$('memberName').value.trim()});finishAdmin(error,'Member saved.');if(!error){ev.target.reset();await load();}}
async function saveCollectionFromForm(ev,prefix=''){ev.preventDefault();if(!isStaff()){formMessage(prefix?'collectionQuickMessage':'adminMessage','Committee login required.',true);return;}const id=prefix?`quick${prefix}`:'';const g=k=>$(id?`${id}${k}`:`collection${k}`);try{const block=g('Block').value.trim(),flat=g('Flat').value.trim();let member=null;if(block&&flat){member=members.find(x=>String(x.block_no).trim().toLowerCase()===block.toLowerCase()&&String(x.flat_no).trim().toLowerCase()===flat.toLowerCase())?.id||null;}const file=g('ReceiptFile').files?.[0]||null,name=g('Name').value.trim(),fileUrl=await uploadDocument(file,'receipts'),payload={festival_id:g('Festival').value,member_id:member,collection_type:g('Type').value,collection_date:g('Date').value,amount:Number(g('Amount').value||0),status:g('Status').value,payment_mode:g('Mode').value,receipt_no:g('ReceiptNo').value.trim()||null,transaction_id:g('TransactionId').value.trim()||null,notes:name||null,block_no:block||null,flat_no:flat||null};if(fileUrl){payload.receipt_url=fileUrl;payload.file_upload_url=fileUrl;payload.receipt_download_url=fileUrl;}const{error}=editingCollectionId?await supabase.from('collections').update(payload).eq('id',editingCollectionId):await supabase.from('collections').insert(payload);formMessage(prefix?'collectionQuickMessage':'adminMessage',error?'Unable to save collection: '+error.message:(editingCollectionId?'Collection updated successfully.':'Collection saved successfully.'),!!error);if(!error){ev.target.reset();editingCollectionId=null;const btn=ev.target.querySelector('button[type="submit"]');if(btn)btn.textContent='Save Collection';const heading=ev.target.closest('.quick-panel')?.querySelector('h3');if(heading)heading.textContent='Add Collection';await load();}}catch(error){formMessage(prefix?'collectionQuickMessage':'adminMessage','Unable to save collection: '+(error?.message||String(error)),true);}}
async function saveExpenseFromForm(ev,prefix=''){ev.preventDefault();if(!isStaff()){formMessage(prefix?'expenseQuickMessage':'adminMessage','Committee login required.',true);return;}const id=prefix?`quick${prefix}`:'';const g=k=>$(id?`${id}${k}`:`expense${k}`);try{const file=g('Bill').files?.[0]||null,billUrl=await uploadDocument(file,'bills'),payload={festival_id:g('Festival').value,expense_date:g('Date').value,expense_name:g('Name').value.trim(),total_amount:Number(g('Amount').value||0),advance_amount:Number(g('Advance').value||0),expense_status:g('Status').value};if(billUrl)payload.bill_url=billUrl;const{error}=await supabase.from('expenses').insert(payload);finishAdmin(error,error?'':'Expense saved successfully.');formMessage(prefix?'expenseQuickMessage':'adminMessage',error?'Unable to save expense: '+error.message:'Expense saved successfully.',!!error);if(!error){ev.target.reset();await load();}}catch(error){formMessage(prefix?'expenseQuickMessage':'adminMessage','Unable to save expense: '+(error?.message||String(error)),true);}}
async function saveScheduleFromForm(ev){ev.preventDefault();if(!isStaff()){formMessage('scheduleCardMessage','Committee login required.',true);return;}try{const{error}=await supabase.from('puja_schedules').insert({festival_id:$('scheduleFestival').value,schedule_date:$('scheduleDate').value||null,event_time:$('scheduleTime').value.trim()||null,title:$('scheduleTitle').value.trim(),description:$('scheduleDescription').value.trim()||null,sort_order:Number($('scheduleOrder').value||10)});formMessage('adminMessage',error?'Unable to save schedule: '+error.message:'Schedule saved successfully.',!!error);if(!error){ev.target.reset();await load();}}catch(error){formMessage('adminMessage','Unable to save schedule: '+(error?.message||String(error)),true);}}
async function editCollection(id){if(!isStaff()){formMessage('collectionQuickMessage','Committee login required.',true);return;}const x=collections.find(r=>String(r.id)===String(id));if(!x){formMessage('collectionQuickMessage','Collection record not found. Please refresh.',true);return;}showPage('collections');const panel=$('quickCollectionPanel'),form=$('quickCollectionForm');if(!panel||!form){formMessage('collectionQuickMessage','Collection edit form is unavailable.',true);return;}panel.classList.remove('hidden');editingCollectionId=x.id;const set=(id,value)=>{const el=$(id);if(el)el.value=value??'';};set('quickCollectionFestival',x.festival_id);set('quickCollectionBlock',x.members?.block_no||x.block_no||'');set('quickCollectionFlat',x.members?.flat_no||x.flat_no||'');set('quickCollectionName',x.notes||x.members?.name||'');set('quickCollectionAmount',x.amount);set('quickCollectionMode',x.payment_mode||'UPI');set('quickCollectionDate',String(x.collection_date||'').slice(0,10));set('quickCollectionStatus',x.status||'Paid');set('quickCollectionReceiptNo',x.receipt_no||'');set('quickCollectionTransactionId',x.transaction_id||'');set('quickCollectionType',x.collection_type||'Flat wise 2026 collection');const btn=form.querySelector('button[type="submit"]');if(btn)btn.textContent='Update Collection';const heading=panel.querySelector('h3');if(heading)heading.textContent='Edit Collection';panel.scrollIntoView({behavior:'smooth',block:'center'});formMessage('collectionQuickMessage','Editing collection. Update the fields and click Update Collection.',false);}
function finishAdmin(error,success){formMessage('adminMessage',error?error.message||String(error):success||'Done',!!error);}
function showPage(name){document.querySelectorAll('.page').forEach(p=>p.classList.add('hidden'));const p=$(`page-${name}`);if(p)p.classList.remove('hidden');document.querySelectorAll('.tab').forEach(t=>t.classList.toggle('active',t.dataset.page===name));}
document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.tab').forEach(t=>t.onclick=()=>showPage(t.dataset.page));$('loginBtn').onclick=()=>{showPage('dashboard');$('authPanel').classList.toggle('hidden');};$('logoutBtn').onclick=async()=>{await supabase.auth.signOut();await refreshAuth();};$('refreshBtn').onclick=load;$('showCollectionForm')?.addEventListener('click',()=>{$('quickCollectionPanel').classList.toggle('hidden');});$('showExpenseForm')?.addEventListener('click',()=>{$('quickExpensePanel').classList.toggle('hidden');});$('showScheduleForm')?.addEventListener('click',()=>{$('quickSchedulePanel').classList.toggle('hidden');});$('collectionSearch')?.addEventListener('input',renderCollections);$('collectionFestivalFilter')?.addEventListener('change',renderCollections);$('collectionTypeFilter')?.addEventListener('change',renderCollections);$('collectionModeFilter')?.addEventListener('change',renderCollections);$('collectionStatusFilter')?.addEventListener('change',renderCollections);$('expenseSearch')?.addEventListener('input',renderExpenses);$('expenseFestivalFilter')?.addEventListener('change',renderExpenses);$('memberForm')?.addEventListener('submit',addMember);$('collectionForm')?.addEventListener('submit',e=>saveCollectionFromForm(e,''));$('quickCollectionForm')?.addEventListener('submit',e=>saveCollectionFromForm(e,'Collection'));$('expenseForm')?.addEventListener('submit',e=>saveExpenseFromForm(e,''));$('quickExpenseForm')?.addEventListener('submit',e=>saveExpenseFromForm(e,'Expense'));$('scheduleForm')?.addEventListener('submit',saveScheduleFromForm);$('authForm')?.addEventListener('submit',async e=>{e.preventDefault();const u=$('loginUser').value.trim().toLowerCase(),p=$('password').value,email=USERNAME_EMAILS[u]||u,{error}=await supabase.auth.signInWithPassword({email,password:p});formMessage('authMessage',error?error.message:'Login successful.',!!error);if(!error){$('authPanel').classList.add('hidden');await refreshAuth();}});load();refreshAuth();});